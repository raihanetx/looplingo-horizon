<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.looplingo.horizon.ui.main.MainFragment"
        android:label="@string/fragment_main_title">

        <action
            android:id="@+id/action_main_to_playbackSettings"
            app:destination="@id/playbackSettingsFragment" />
    </fragment>

    <fragment
        android:id="@+id/playbackSettingsFragment"
        android:name="com.looplingo.horizon.ui.settings.PlaybackSettingsFragment"
        android:label="@string/fragment_playback_settings_title">

        <argument
            android:name="videoPath"
            app:argType="string" />
        <argument
            android:name="contentUri"
            app:argType="string"
            android:defaultValue="" />
    </fragment>

</navigation>
